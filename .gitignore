# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Build artifacts
*.egg-info/
dist/
build/

# Frontend build outputs (regenerated by `npm run build` and bundled into the wheel)
web/build/
web/.svelte-kit/
web/node_modules/
src/ai_almanac/server/static/

# Data
.env
scripts/backend-env.yaml
*.db
*.db-shm
*.db-wal
job_outputs/

# Data archives
*.zip
*.tar.gz
*.tar.bz2

# Editor / OS
.DS_Store
.idea/
.vscode/
# pixi environments
.pixi/*
!.pixi/config.toml

# Terraform — state, provider plugins, plans, and secret-bearing var/backend files.
# .terraform.lock.hcl is intentionally tracked.
terraform/.terraform/
terraform/*.tfplan
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.tfvars
terraform/backend.hcl

# Local tooling / scratch
.localrag/
__marimo__/
