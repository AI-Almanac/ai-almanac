name: deploy-staging

# Builds the single ai-almanac-web image (FastAPI + bundled SPA), runs DB
# migrations, then rolls the staging backend to the new revision. Production
# runs the same single-image architecture; deploy-prod.yml is the mirror of
# this workflow targeting almanac-backend.

# Only paths that actually ship in the ai-almanac-web image trigger a deploy;
# workflow_dispatch stays as the escape hatch for everything else (docs,
# terraform, CI config, etc.).
on:
  push:
    branches: [develop]
    paths:
      - "src/**"
      - "modal/**"
      - "web/**"
      - "Dockerfile"
      - "pyproject.toml"
  workflow_dispatch:

concurrency:
  group: deploy-staging
  cancel-in-progress: false

env:
  REGION: us-central1
  AR_IMAGE: us-central1-docker.pkg.dev/ai-almanac/almanac/ai-almanac-web
  SERVICE: almanac-backend-staging
  MIGRATE_JOB: almanac-migrate-staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Authenticate to GCP
        id: auth
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          workload_identity_provider: projects/251618530185/locations/global/workloadIdentityPools/github/providers/github-oidc
          service_account: almanac-ci@ai-almanac.iam.gserviceaccount.com
          token_format: access_token

      - uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3.0.1

      - name: Log in to Artifact Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: us-central1-docker.pkg.dev
          username: oauth2accesstoken
          password: ${{ steps.auth.outputs.access_token }}

      - uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1

      - name: Build and push
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: .
          push: true
          platforms: linux/amd64
          # web/.env must stay out of the build (it pins VITE_API_URL to
          # localhost); .dockerignore handles that. The SPA reads its API base
          # from runtime /config.js, so VITE_API_URL is intentionally unset.
          #
          # These are hardcoded (not repo `vars`) on purpose: they're public,
          # staging-specific constants, and the redirect URL must EXACTLY match
          # the Globus client's registered redirect for this origin. A stale
          # repo var here bakes a wrong redirect, and Globus then fails the
          # token-exchange CORS check ("NetworkError when attempting to fetch
          # resource") in an endless login loop. The redirect must equal the
          # origin the SPA is served from (the staging LB domain).
          build-args: |
            VITE_GLOBUS_CLIENT_ID=c0309fe2-a701-45fa-bf28-1e4fed6c5f1a
            VITE_GLOBUS_REDIRECT_URL=https://staging.ai-almanac.org/callback
          tags: |
            ${{ env.AR_IMAGE }}:latest
            ${{ env.AR_IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # The backend calls these apps by name in its own Modal environment, so
      # they ship with every backend deploy and never drift behind it.
      # Installs the Modal CLI from pixi.lock (hash-pinned) rather than a fresh
      # PyPI resolve, since this step holds the Modal tokens.
      - uses: prefix-dev/setup-pixi@d3f436a425481402e6a95a1d1fc10331c708cd9e # v0.10.2
        with:
          environments: default
          cache: true

      - name: Deploy Modal apps
        env:
          MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
          MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
        run: |
          for app in app blending_app forecasts_app; do
            pixi run --locked python -m modal deploy --env staging "modal/$app.py"
          done

      # Migrate before routing traffic: new code may need new schema. The job's
      # image is pinned in Terraform with ignore_changes, so point it at this
      # build, then run it to completion.
      - name: Run database migrations
        run: |
          gcloud run jobs update "$MIGRATE_JOB" \
            --region "$REGION" \
            --image "$AR_IMAGE:${{ github.sha }}"
          gcloud run jobs execute "$MIGRATE_JOB" --region "$REGION" --wait

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@2028e2d7d30a78c6910e0632e48dd561b064884d # v3.0.1
        with:
          service: ${{ env.SERVICE }}
          region: us-central1
          image: ${{ env.AR_IMAGE }}:${{ github.sha }}

  # Post-deploy verification (SDLC Phase 3): readiness + smoke tests + error
  # log soak via scripts/post_deploy_check.sh (all cloud-specific commands
  # live in the script). On failure, Claude triages the captured errors
  # against the deployed diff into the workflow summary.
  verify:
    needs: deploy
    runs-on: ubuntu-latest
    environment: staging
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Authenticate to GCP
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          workload_identity_provider: projects/251618530185/locations/global/workloadIdentityPools/github/providers/github-oidc
          service_account: almanac-ci@ai-almanac.iam.gserviceaccount.com

      - uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3.0.1

      - name: Smoke test and log soak
        run: ./scripts/post_deploy_check.sh "$SERVICE" "$REGION" "https://staging.ai-almanac.org" 300

      - name: AI triage on failure
        if: failure()
        uses: anthropics/claude-code-action@cfc3eb22bfed5c26ef66e3223c982af27e4524de # v1.0.231
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: |
            The staging deploy of commit ${{ github.sha }} failed post-deploy
            verification. Captured error logs (if the check reached the log
            scan) are in post-deploy-errors.txt. Inspect them and the
            recently deployed changes (git log/diff of the last few commits)
            and produce a concise triage: most likely root cause, the
            specific commit/change implicated if identifiable, and a
            recommendation — rollback (see docs/sdlc/rollback.md) or
            fix-forward. Append your analysis in markdown to the file whose
            path is in the GITHUB_STEP_SUMMARY environment variable.
          claude_args: >-
            --max-turns 12
            --allowedTools "Read,Grep,Glob,Bash(git log:*),Bash(git diff:*),Bash(git show:*),Write"
